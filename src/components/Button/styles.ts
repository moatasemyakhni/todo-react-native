import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    iconBtn: {
        height: 60,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%',
        borderRadius: 5,
    },
    iconText: {
        fontWeight: 'bold',
        fontSize: 24,
        color: 'white',
        marginLeft: 10,
    },
});